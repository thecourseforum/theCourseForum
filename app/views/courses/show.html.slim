- content_for :page_heading, "#{@course.title} (#{@course.mnemonic_number})"

= stylesheet_link_tag "courses"
= react_component("courses/CoursePage", {course_id: @current_course_id, graph_data: @course.get_grade_percentages(@professor ? @professor.id : -1),
      ratings: @rev_ratings})
